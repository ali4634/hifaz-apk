name: Build Android APK

on:
  push:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      # 1. Code ko download karna
      - name: Checkout code
        uses: actions/checkout@v4

      # 2. Node.js set up karna
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      # 3. Java set up karna
      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: 'zulu'
          java-version: '17'
      
      # 4. Tamam packages install karna (Iske baad capacitor.js mojood hoga)
      - name: Install dependencies
        run: npm install

      # 5. YEH HAI JADUI AUR HATTMI HAL
      # capacitor.js ko seedha uski factory (node_modules) se www folder mein copy karna
      - name: Copy capacitor.js from node_modules to web dir
        run: cp node_modules/@capacitor/android/capacitor.js www/capacitor.js

      # 6. Android platform ko add karna
      - name: Add Android platform
        run: npx cap add android

      # 7. Ab project ko sync karna (Ab use capacitor.js mil jayega)
      - name: Sync Capacitor project with Android
        run: npx cap sync android
      
      # 8. APK banana
      - name: Build APK
        working-directory: android
        run: ./gradlew assembleDebug

      # 9. APK ko upload karna
      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: hifaz-tracker-apk
          path: android/app/build/outputs/apk/debug/app-debug.apk
